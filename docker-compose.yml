services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    image: ghcr.io/sunmh207/ai-codereview-gitlab:1.3.1
    ports:
      - "5001:5001"
      - "5002:5002"
    volumes:
      - ./biz:/app/biz
      - ./conf:/app/conf
      - ./data:/app/data
      - ./log:/app/log
      - ./api.py:/app/api.py
      - ./ui.py:/app/ui.py
    env_file:
      - ./conf/.env
    restart: unless-stopped
volumes:
  redis_data: